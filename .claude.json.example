# Claude MCP Configuration

The Supabase MCP server is configured in your local Claude desktop config:
`~/Library/Application Support/Claude/claude_desktop_config.json`

This provides direct database access for admin operations.
Never commit .claude.json to the repository.
